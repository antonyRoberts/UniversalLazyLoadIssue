//Component for Page Not Found, can Make Pretty in the future.
import { Component } from '@angular/core';

//try with article instead of div?
@Component({
   // Do Not Use ModuleId with Webpack... moduleId: module.id,
   selector:'easy-404',
   template:`
    <div>
    </div>
   `
})
export class PageNotFoundComponent {}